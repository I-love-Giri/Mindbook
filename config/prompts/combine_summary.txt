You are given several JSON summaries generated from different parts of the same YouTube transcript.

Merge them into a single JSON summary.

Instructions:

- Merge duplicate or closely related topics.
- Preserve all important information.
- Remove repeated facts.
- Merge and deduplicate keywords.
- Produce a concise overview of the entire transcript.
- Keep the same JSON structure.
- Return ONLY valid JSON.
- Do NOT include markdown or explanations.

JSON summaries:

$summaries
